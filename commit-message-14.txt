fix: wire up PMCC sort/filter -- three real dead-guard bugs found and fixed

Real, complete work closing the gap flagged at the start of this
ticket ("absolutely not filters on this page for anything useful").
Traced through to three separate instances of the identical bug in
app/screener/page.tsx: activePmccSession ? oldPath : realLogic, added
at three different points, that had made PMCC's entire results
controls, sorting, and filtering unreachable since the day PMCC's UI
was added.

1. The whole pmcc-result-controls section (OI floor input + sort
   buttons) never rendered at all -- confirmed via git log -S that
   this branch was dead code from the moment it was added
   (2026-08-15, c6dda04). This produced the screenshot with zero
   visible controls that started this whole thread.
2. sortItems() was unconditionally bypassed for PMCC sessions --
   results were never actually sorted by anything, the exact
   "Contract order" complaint the ticket exists to fix.
3. The OI eligibility filter itself was ALSO bypassed, separately
   from the other two -- the floor control rendered and the sort
   worked, but the floor never actually excluded anything. Found only
   because real test coverage kept failing after the first two fixes
   were already verified correct in isolation.

Also: corrected a factually inaccurate OI tooltip (claimed only the
short call mattered; the real code -- getLegOiSet -- requires both
legs, confirmed via direct read). Extended the shared SortField/
SortableMetrics system with three new PMCC fields (widthMinusDebitPct,
breakevenPct, annualizedRoiPct) after catching and avoiding a real
mistake in the original plan: almost repurposing bestCandidate.roc
(a load-bearing pass/fail qualification gate used in checklist.ts,
rank-scoring.ts, and page.tsx's own checklist row) to carry PMCC's
much larger annualized figure, which would have silently weakened an
unrelated safety check. Added a narrow, team-reviewed default-sort
reset: switching to a PMCC scan only resets the sort if it's
currently on a field structurally meaningless for PMCC
(score/pop/creditPct, all null for a debit structure), never
overriding a PMCC-relevant choice already made.

Real test coverage added (app/screener/__tests__/
PmccResultCardFields.test.tsx), reusing pmccProduction.test.ts's
real, proven fixture pattern. Writing it surfaced six further genuine
bugs, all in the test fixtures themselves, none in the app: a
candidate-ID collision, an OCC-symbol/strike mismatch, a
publishedOrder/pmccPairingCounts consistency violation (real
validation correctly rejecting bad test data), a spread-quality
threshold broken at low option prices, an incorrect assumption about
how the pairing engine retains multiple candidates (one long leg,
multiple short strikes -- matching real PMCC usage and the original
screenshot exactly), and confusion between the scan's own OI criteria
and the UI's separate post-scan floor control. Each traced to a
precise, confirmed root cause via direct debugging, not guessed.

Verified: all 4 new tests pass. Full suite: 176/176 files, 2522/2522
tests, genuinely clean. Zero leftover debug instrumentation anywhere
in the diff.
