# Autopilot Config API

The intended canonical config endpoint is:

```text
/api/autopilot/config
```

If a stray probe file appears in this folder during early Sprint 1A work, remove it before Sprint 1A is marked complete.
