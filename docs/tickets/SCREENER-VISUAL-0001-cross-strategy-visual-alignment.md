# SCREENER-VISUAL-0001 — Cross-strategy visual alignment of scan configuration

## Status

**Draft.** Deferred by Dean 2026-09-23. Pick up alongside the CC, PMCC, and LEAPS phases of `SCREENER-CONFIG-0001`.

## Problem

The v5 CSP mock used its own dark and amber look, not the app theme or the `ScanModalShell` chrome. The Rank and Targeted controls should look like the other strategies' configuration screens.

## Scope

- Decide one shared look for registry-driven controls (pills, inputs, tags, summary panel) using the shell theme.
- Diane revises the mocks on the real theme; Spreads and CSP conform first, then CC, PMCC, and LEAPS as they migrate.
- Add screenshots per strategy configuration and applied-rules receipt (acceptance criterion 9 of `SCREENER-CONFIG-0001`).

## Notes

- Behavior, labels, and lifecycles are settled in `SCREENER-CONFIG-0001A`; this ticket changes visuals only.
