# DECIDE-0001 review (2026-09-25)

**Reviewed:** `docs/tickets/DECIDE-0001-decisive-position-recommendations.md` as merged in `d311fd8`.
**Reviewer:** the working session, applying the Ian, Alan, Quinn, Paul and Diane lenses inline. This is not an independent-agent review. Because the ticket changes risk logic, an independent second opinion is recommended after the ticket is revised.
**Verdict:** **Do not build yet.** The design shape is sound (pure rules, code decides, AI explains, everything logged). Three findings change what would be built and must be settled first. Three more are serious but do not change the design.

## Decisions by Dean (2026-09-25)

1. **Stop level: keep production's.** The stop trigger stays at a mark of 2× the credit received (a loss of 1× the credit, `DEFAULT_ENTRY_STOP_MULTIPLE = 2`). The ticket's `mark ≥ 3·C` rows and Alan's fixtures that pin 3× must be rewritten to `mark ≥ 2·C`.
2. **Sequencing: proceed with DECIDE-0001 now; do CUT-LOSSES-REVIEW-0001 after.** The calm-presentation requirement in finding 3 still applies to DECIDE-0001's UI (state reasons, flip conditions and what the position needs; no red command).

Still required before build: the intent branch (finding 2), the data-prerequisites phase (finding 4), the entry-support decision (finding 5), reuse of `lib/decision-review` (finding 6), then an independent Ian and Alan agent review.

## Blocking

### 1. The stop is defined differently from production (Ian, Alan)

The ticket says: stop when `mark ≥ 3·C`, "a loss of 2× credit".

The code says otherwise:

- `lib/portfolio/stopLossPolicy.ts:184`: `DEFAULT_ENTRY_STOP_MULTIPLE = 2`, and `buildOriginalCreditDefaultPolicy` sets `triggerPrice = credit × 2`. That is a mark of 2× credit, a loss of 1× credit.
- `lib/portfolio-intelligence/policies/defaults.ts:16`: `materialLossPct: -100` (a loss of 100% of the credit, the same point).
- `docs/ROADMAP.md` ES-0003: Stop Limit "trigger 2x credit".

Adopting the ticket as written would silently loosen every stop by one full credit. **Decision needed:** keep production's convention (mark ≥ 2·C) and rewrite the ticket and Alan's fixtures, or make a deliberate, recorded decision to loosen it (and change the stop-loss wiring to match). Recommendation: keep production's. The phrase "2x credit loss stop" in CLAUDE.md is ambiguous and should be corrected to say which.

### 2. It ignores position intent (Ian)

POSITION-INTENT-0001 (built 2026-09-25) stores Income / Acquire / Wheel for lone short puts and calls. In this ticket a CSP or CC gets `CLOSE_STOP` (rule 2) and `CLOSE_BROKEN` (rule 3) whatever its intent.

Dean's stated position (2026-09-25): a CSP has an Acquire or a Wheel aspect, so a strong recommended action does not make sense for it. The existing recommendation code already follows this for Acquire and Wheel (`lib/portfolio-data/acquisition.ts:2314`: hold for assignment, no breach or stop exits).

The riskiest case is the CC rule "close both the call and the shares": it recommends selling the shares, and it ignores whether the shares are held as Hold or Wheel (stock intent now exists).

**Required change:** add an intent branch. For Acquire and Wheel, CSP and CC return `ACCEPT_ASSIGNMENT` or `HOLD` with the context shown, never `CLOSE_STOP` or `CLOSE_BROKEN`. Ian decides whether a hard loss limit still applies to them (he did not rule on this). The broken-stock signal may still be shown as information.

### 3. It runs against Dean's report on Cut Losses (Ian, Paul)

Dean reported that a loud "Cut Losses" led to panic closes on positions that later recovered. The Trade Log analysis (CUT-LOSSES-REVIEW-0001) found 21 of 28 losses were closed before reaching 2× credit and only one position was held near expiry. Ian's ruling: soften presentation and capture data now; no scoring change until outcome data exists.

This ticket makes recommendations more forceful and more certain. It should not start ahead of CUT-LOSSES-REVIEW-0001, and it must require the calm presentation (state the reasons, the flip conditions and what the position needs, not a red command). Paul: sequence it behind that ticket, or record Dean's explicit decision to proceed.

## Serious

### 4. The data the signals need does not exist (Alan, Quinn)

- **Trend break** needs 201 daily bars. `app/api/chart/route.ts:14` requests `range=6mo` (about 125 bars), so the rule could never fire. It needs at least a 1-year range.
- **Support break** needs volume. The chart route builds bars with only `t, o, h, l, c` (line 51); there is no `v`. The route would have to return volume.
- The chart data source is Yahoo, and the ticket says nothing about its adjusted-versus-unadjusted closes; Alan flagged this for RSI-TURN-0001 too.

### 5. Support at entry does not exist for current positions (Quinn)

`S` is "frozen when the position opens and stored in the entry record". Positions opened before 2026-09-25 have no entry snapshot (Trade Log CSV: the Score, Entry State and Entry Overrides columns are empty on all 65 rows). Every existing position would show this signal as "not evaluated". Decide: backfill from history at first evaluation (computed from bars before the entry date, labeled as reconstructed) or accept NE for old positions.

### 6. It duplicates the log that already exists (Quinn, Paul)

`lib/decision-review` (PI-0008C Decision Outcome Tracking) already has a `DecisionReview` with a trader-action vocabulary and outcome status. Dean's console query on 2026-09-25 showed **0 stored reviews**, so nothing is writing to it. DECIDE-0001D should extend or replace that module on purpose, and first find out why nothing writes reviews.

## Other notes

- **Three engines, not two (Quinn).** The ticket says keep both rule engines. There are three that shape what the trader sees: the older `getRecommendation` in `lib/portfolio-data/acquisition.ts` (the source of today's Hold and Cut Losses, and of the Acquire skip), the portfolio-intelligence engine (`managementIntent.ts`, `positionObjective.ts`), and the LEAPS engine. Open item O2 ("confirm the stop-loss wiring file") is answered by `acquisition.ts` plus `lib/portfolio/stopLossPolicy.ts`.
- **Scope (Paul).** One epic with five phases and an AI prompt rewrite is large. Suggest 0001A (signals, information only, no actions) as the first slice, after the data in item 4 exists, because it can ship as display without changing any recommendation.
- **AI-POLICY (Paul).** The amendment to D2 and D9 should be recorded before 0001C. Note that the LEAP dashboard built on 2026-09-25 states "none recommends an action" (`lib/leaps-analysis/dashboard.ts` header); the ticket should say whether that principle changes.
- **Confidence rules (Alan).** The ticket's confidence bands (margin under 10% gives LOW) are reasonable, but "MEDIUM: another rule points the other way" needs an exact definition and fixtures.
- **CSP stop and Acquire (Alan).** Once item 2 is settled, the fixtures for CSP and CC must be duplicated per intent.
- **UI (Diane).** The mock (O1) should show a recommendation beside the intent chip and the extrinsic and value rows now on the Positions table, and use the calm presentation from item 3.
- **Finnhub (Quinn).** New external dependency; the server-only environment variable rule (SEC-0001) is right. Add a failure mode: when the feed is down the fundamental signal is NE, not clear.
- **Fees (Alan).** The fee constant "from the broker's published schedule, rounded up" should be named in one place with its source and date.

## What the ticket author should do next

1. Settle item 1 (stop convention) and rewrite the stop rows and Alan's fixtures.
2. Add the intent branch (item 2) with Ian's ruling on hard limits for Acquire and Wheel.
3. Add the sequencing or an explicit decision from Dean on item 3, and require calm presentation.
4. Add a "data prerequisites" phase (chart range, volume, entry support backfill) before 0001A.
5. Decide reuse of `lib/decision-review` for 0001D.
6. Then request an independent agent review (Ian and Alan lenses) before build.

## What is good and should be kept

- Pure, deterministic evaluators with explicit inputs and a property test.
- `BLOCKED` is never `HOLD`, and confidence is null only when blocked.
- The AI cannot change the action; a server-side exact-match check discards a mismatch.
- The 21-DTE exception for CSP and CC (matches the CLAUDE.md change and the portfolio prompt).
- "Winners never roll", the fee-aware qualifying roll, and the wide-quote two-reading stop.
- Golden fixtures at every threshold boundary.

## Round 2: independent lens reviews of revision 2 (2026-09-25)

All five ran as independent read-only agents. Full required changes are folded into revision 3 of the ticket.

- **G1 Ian: approve with changes.** O5 resolved (no hard loss limit for Acquire or Wheel; context lines instead). O6: keep the no-action behavior for the unclassified default but show "Acquire (default, not chosen)" with a one-tap "Choose intent"; Income must be an explicit choice. Spread row 6 must not return `CLOSE_STOP` without a stop trigger; profit ranks above broken stock; broken stock is gated on losing or short delta ≥ 0.30; a covered call never gets `CLOSE_STOP` or `CLOSE_BROKEN`; LEAPS long-leg actions need intent and a short-call guard (most dangerous gap: closing the long leg under an open short call); the trend signal must not fire if it was true at entry; the newer engine's material loss (100) beats assignment preferred (90) and must be suppressed for Acquire and Wheel.
- **G2 Alan: approve with changes.** 2x arithmetic and fixtures redone; unreachable stop is `2C ≥ width` and per IC wing; float traps (use integer math); two confidence band edges wrong; CC annualized-return roll formulas replaced by `rollNet_c · DTE0 > c0_c · ΔDTE`; failed-gap window wording; O8 ruled (split-adjusted, dividend-unadjusted `quote` closes, never `adjclose`); O9 ruled (write-once reconstructed support, 20 bars strictly before the NY entry date).
- **G3 Quinn: do not build yet.** `getRecommendation` has no production caller; there is one live engine; cutover map of about ten displays; slice order S0 to S4; do not change `GET /api/chart` (ten callers; would change scan trend numbers and break `RsiLine.test.tsx`); stop trigger must be an input from the trusted policy and confirmation must reuse `evaluateStopBreach`; determinism needs `now` as an input; decision-review has 0 records because nothing auto-writes; 0001C names the wrong routes and removing `disclosure` breaks the screener; LEAPS long has no engine.
- **G4 Paul: approve Phase 0 and 0001A only.** Split 0001B into a shadow run and a cutover; defer 0001C and 0001E to their own tickets; this epic owns the log and calm presentation, so CUT-LOSSES-REVIEW-0001 shrinks; reword the outcome to "a calm action or a plain hold"; new open-position AI route rather than rewriting frozen routes; D9 is Ian's.
- **G5 Diane: approve with changes.** No red anywhere (including the red Cut Losses button and the badge's red and orange classes); label and dot table; one reason plus one "changes if" line; collapsed detail; Acquire and Wheel read "Hold" with amber context lines; standing disclaimer at the foot. A rendered mock is still needed for Dean.
- **G6 Dane: pending.**

**One correction to this document's round 1:** finding 2 said the existing recommendation code "already follows" the Acquire exemption. That is true only of `getRecommendation`, which the app never calls (Quinn, verified). The live path does not follow it.
