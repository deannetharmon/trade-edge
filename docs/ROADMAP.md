# TradeEdge Roadmap (maintained by Paul — update after every major decision)

Last updated: 2026-09-25 (after `76da9cb`)

## Done this session (2026-09-24; all merged to main, production deploys green)

SCAN-ALIGN-0001 slices A `PMCC-HELD-BREAKEVEN-0001` and 0001B-1 held-card outcomes (`c26c1b1`), B `PMCC-EARNINGS-PAST-0001` (`5ee2cfc`), C1 OI policy and C2 hybrid bid/ask with the $0.50 short-call ceiling (`a44b47c`), D earnings removal and after-expiry warning (`610bdd9`), E debit below width always on (`d0978b2`), F1 delta chips (`133c842`), F2 PMCC short delta hard filter, LEAPS-ADVISOR-0001B waived (`a34098d`); PMCC-RECEIPT-0001 part 2 (delta window on the status line, `bd49f63`) and part 1 (Active PMCC rules line, `116f47e`); SCAN-GUIDE-0001 part 3 (PMCC width help lines, `116f47e`). Permanent rule: a held LEAP with quantity != 1 fails closed (multi-lot broker averaging unverifiable; Dean holds single-contract LEAPs). All of SCAN-ALIGN-0001 is complete; the PMCC registry migration's prerequisites (A through F) are met.

## Now (nothing building)

Merged since the last update (all production deploys green): small-ready bundle (`d63801a`: CC earnings re-screen button on disqualified rows, held pair lookup applies the breakeven floor, shared `warmScreenerPage` helper, readiness label from failing gates only, inline invalid-delta message); **EARNINGS-DATEBASIS-0001** (`50fab51`: New York basis for earnings/expiry-inclusion math at every earnings site; `scan-utils.daysUntil` untouched, so expiration-window DTE stays on the old basis: known temporary split, needs its own ticket); tiny hardening (`48649e0`: warm-up for `CspDefaultDeltaFilter`, strict-expiry boundary tests, 30 OTOCO guard tests, deleted the dead `ttPost` and `buildOpenSpreadOrder`). EARNINGS-PRECHECK-0001 is accepted (Ian ruled, coupling proven, Quinn CSP path confirmed).

Open items:
- **DEPRIORITIZED by Dean 2026-09-25:** Rinse & Repeat is not a priority feature and the wheel feature is not needed yet (both parked). Do not run Rinse & Repeat live meanwhile (see ES-0003). Focus: verify the core scan, portfolio and order flows work first.
- **ES-0003 OTOCO stop child (Ian ruled 2026-09-25: REJECT the plain `Stop` on a multi-leg close; proof first).** Do NOT run Rinse & Repeat live until Dean records the current-shape accept/reject response (dry-run/cert/paper order; broker calls are browser-side). Then a small ticket to switch to Stop Limit (trigger 2x credit, limit trigger x 1.10 capped at width minus credit, `price-effect: Debit`) with fail-closed tests. Details in the ES-0003 ticket. Tests and dead-code removal already merged (`48649e0`).
- **LCC-0001A Gate A:** blocked on a real Covered Call shadow-parity sample; do not start LCC-0001B before it.
- **SCAN-EARNINGS-TARGETED-0001 DONE 2026-09-25:** Targeted (strict) scans decide earnings per expiration and require earnings 10+ calendar days after expiry (Ian, revised after an independent review and source checks; Dean chose 10). Follow-ups filed: **EARNINGS-MARGIN-0001 DONE 2026-09-25** (10-day earnings buffer after expiry live in Targeted spreads, CSP and covered call (the main Ranked scan does NOT have it yet: it recomputes earnings locally in `rank-scoring.ts`; fixed in QUAL-STATES-0001 phase 0, 2026-09-25); PMCC deliberately left on the SCAN-ALIGN-0001D ambient-tag model by Dean), the `lib/screener.ts` duplicate `runChecklist`, red "Earnings in Nd" text that contradicts a passing check, OI floor/target wording, stale past earnings dates (MRVL, ORCL), and the FMP plan gap (partial coverage; `/api/event-risk` degraded). Also merged 2026-09-25: LEAPS PDF summary table export (default) and outlined Export PDF button. Personas are now inline lenses (`docs/PERSONA_LENSES.md`).
- **STRATEGY-COVERAGE audit DONE 2026-09-25 (Trade Log, Performance, portfolio prompt):** every report that groups by strategy now covers all of them. Fixed: Trade Log strategy filter and by-strategy summary (CSP, short call and other spread were missing), Performance "P&L by Strategy" widget and AI prompt (CSP and short calls were missing; now one shared breakdown, `lib/tradeLog/strategyBreakdown.ts`), portfolio AI prompt strategy mix (was BPS/BCS/IC/Other, now every strategy, `lib/portfolioReview/strategyMix.ts`). Guard tests: rows must add up to the trade count. Already sound: the Strategy Performance report (catch-all row), the portfolio review composition (generic), scan exports and CSVs (generic). Not covered by design: Income Engine (ES-futures spreads only), Paper Trading (supports CSP, BPS, BCS, IC only). Known small gaps: the entry-evidence rollup counts CSP as "no snapshot"; the scan CSV's Qualified column is yes/no and does not show Caution.
- **EM-CONTEXT-0001 DONE 2026-09-25:** expected move is information only (never a fail, warn or gate); wrong "POP below 68%" text corrected (about 85% at the boundary); EM shown neutrally next to OTM% on the card; order-window advisory reduced to one neutral line; build-failing survival test added. Optional control and "find next best outside EM" not built (Dean's call, if he misses them). Ticket: `docs/tickets/EM-CONTEXT-0001-expected-move-is-information-only.md`.
- **LEAPS-DEFAULTS-0001 DONE 2026-09-25:** LEAPS screen defaults to a 12 to 18 month window held in one variable (`lib/scans/leapsEntryTargets.ts`), PMCC short call defaults to 30 to 45 DTE; held-LEAP evaluation bounds and all scoring unchanged. Ticket: `docs/tickets/LEAPS-DEFAULTS-0001-default-leap-window-variable.md`.
- **QUAL-STATES-0001 (approved 2026-09-25):** Qualified / Caution / Disqualified across scans, override kept and recorded through to the trade log. All phases DONE (Ranked scan can produce qualified rows; three-state badge, borders and counts on spread cards; order-window acknowledgment, TRADE THIS variants and the override recorded through to a Trade Log chip and CSV columns for spreads, iron condors (their promotion was missing and is fixed) and cash-secured puts via entry notes). Covered call, PMCC and LEAPS wait until asked. Follow-up candidate: stop re-promoting already-promoted pending entries on every Trade Log load. Ticket: `docs/tickets/QUAL-STATES-0001-three-qualification-states.md`.
- **SCAN-HEADER-0001 DONE 2026-09-25:** every scan header leads with QUALIFIED / DISQUALIFIED counts and one "scan Nm ago" provenance chip, including LEAPS (`docs/tickets/SCAN-HEADER-0001-standard-scan-header.md`).
- **METRIC-MOVEMENT-COLORS DONE 2026-09-25 (Ian's rules by position type; display only):** Metric Movement now has Int (intrinsic) and Ext (extrinsic) rows for every option position, split from the trader's side (a bought option is the value you own; a sold option or credit spread is the value you owe, and its extrinsic reads "Extrinsic left" = what is left to earn; spread legs are netted). Colors: bought options (LEAP, long call/put, debit spread, PMCC) intrinsic up green, extrinsic up green, down red; sold options (CSP, short call, credit spread, iron condor) intrinsic up RED, extrinsic up RED and down green (the decay is the profit); IV follows the sign of net vega (up green if long vega, red if short vega); EXCEPTION a lone short put set to Acquire or Wheel keeps intrinsic neutral (assignment is the plan). Delta, gamma, vega and IVR stay neutral; theta and P/L unchanged. Ext and Int compare to ENTRY, the other rows to the first tracked snapshot. Known small item: `lib/scans/__tests__/ccConfigTruthfulness.test.ts` fails late in the UTC day (its date helper is UTC-based; the code under test uses the old daysUntil basis): it passes again after 00:00 UTC.
- **COLUMN-RENAME DONE 2026-09-25:** the Positions table column "Strike Gap" is now "Price Change" (it shows both the price move since entry and the distance to the strike; column id `underlying` unchanged, so saved column choices are unaffected).
- **ACQUIRE-WHEEL-LIVE-0001 DONE 2026-09-25 (Dean: fix now if Ian is aligned; Ian approved with changes, all applied):** the recommendation the app actually shows (`scorePortfolioPositionObjective` -> `evaluatePositionObjective`) now honors Acquire and Wheel for a LONE SHORT PUT only (`assignmentPlanned`, set in `acquisition.ts` from `pos.intent`): a paper loss (material loss and weak-health loss) no longer produces Cut Losses or reaches intent selection, the 21-DTE roll-soon suggestion is skipped, earnings-risk is a medium note ("Assignment is the plan"), assignment preference counts only with a real ITM or tight-buffer signal (so a calm OTM put stays Hold or Take Profit, per Ian), and a plain context line states the loss ("Set to Acquire: assignment is the plan, so no loss exit is suggested. Down 150% of credit (1.5x the credit received)."). Unchanged: Income and neutral puts, short calls, spreads, bought options, place-gtc, close-winner, assignment-risk. Note the earlier change to `getRecommendation` (Wheel like Acquire) had no effect because the app never calls that function. Every unclassified lone short put defaults to Acquire, so it now has no loss exit until Dean chooses Income (accepted by Dean 2026-09-25; the position should show "Acquire (default, not chosen)", which is still to be built). Tests: `lib/portfolio-data/__tests__/assignmentPlannedRecommendation.test.ts` (14, run against the live path; 6 fail on the old code).
- **ROADMAP-ORDER 2026-09-26 (Paul):** next two weeks: Dean's decisions; S3-0 (one working copy of `page.tsx`); the POSITION-INTENT chip and "Choose intent" link (owned by POSITION-INTENT-0001; needs a defaulted-or-chosen flag in the store); RSI-TURN-0001's live-bar timestamp fix (before DECIDE-0001 S0; remaining RSI popups after S3-0); S0 then S1; S3-1 after Ian; the CUT-LOSSES-REVIEW-0001 MAX_LOSS read-only check, then close it (its capture moves to DECIDE-0001D).
- **CHART-LINK-PLACEMENT DONE 2026-09-25 (`76da9cb`):** the "chart" link moved from the Strike Gap column to the last line of the Position column on the Positions table, and every row on the Portfolio tab has the same link as its third line (the row is now a div with an inner select button; clicking the link does not change the selected symbol).
- **EARNINGS-NO-DATE-0001** (Ian ruling 2026-09-24): a missing earnings date is NOT at-risk (passes with "Earnings date unavailable", so ETFs and indices stay clean); a present-but-unparseable date should be labeled distinctly ("Earnings date unparseable") or fail toward at-risk (log first if it currently shows the same text as the ETF case; not worth a hotfix); a missing `asOf` fails toward at-risk; any caution tag is ambient weight, never a decision-tier fail.

## Next (recommended order; scoped or ready to scope)

~~0. **EARNINGS-DATEBASIS-0001 (P1, ruled by Ian 2026-09-24, after the small-ready bundle):** shared `daysUntil` (UTC-midnight rounding) makes earnings day read "Already reported" after ~12:00 UTC so the checklist skips the fail/warn (a qualified trade can sit through an earnings-day print with a green check); same bug in the expiry filter and rinse-repeat; fix ~11 sites on the New York basis (`daysUntilNy`). Autopilot `recommendationEngine.ts:147` is its own reviewed item (order path). Alan fixtures, Quinn review. Ian + Alan signed off (fixtures in the ticket); build after the small-ready bundle. Known temporary split: earnings/expiry-inclusion math on the NY basis, DTE-window membership (shared `daysUntil`) on the old basis until its own ticket (expiration DTE uses in checklist, screener page, pmccChainClient, cc-tracker, wheel-simulator).~~ DONE

~~1. **CC and CSP earnings pre-check** (likely Dean's false earnings warning): `page.tsx` ~:1468 (CC) and ~:1280 (CSP) compare local-time `daysUntil` against the scan DTE range, not the contract's own expiry, and the fail stays visible when no candidate exists. Changes CC/CSP behavior, so Ian rules first; small ticket (Paul drafts). Bundle with the other local-time or UTC earnings comparisons: `pmccReadiness.ts:29`, `pmccScore.ts:83`, `pmccStopGtcPrompt.ts:27`, `page.tsx:3880/:8190`, `portfolio/page.tsx:2341`, `checklist.ts`, `covered-call-finder.ts:212`, `screener.ts`, `rinse-repeat/page.tsx:1395`, `csp-finder.ts:161`; the New York date helper `lib/scans/pmccEarningsDates.ts` (from B) is the model.~~ DONE
~~2. **`page.tsx:5685` held pair lookup** does not apply the breakeven floor (correctness gap in slice A's area): small ticket to Frank/Paul.~~ DONE
3. **SCAN-GUIDE-0001 parts 1-2** (after-scan "Width rule removed N of M short calls (ceiling: X, percent rule: Y)" line and a muted pointer when the rule removed most of a symbol's candidates; the "K more would qualify at $0.75" clause was removed by Ian). Paul holds them until the pointer's own weight check on a rendered mock. Needs CC rejection tagging at the width gate (engine data).
4. **PMCC-HELD-BREAKEVEN-0001C** (P2): held-LEAP results tiles (Avg open, Since open with em dashes), results container, "Show full breakdown", filter funnel with engine funnel data, the "Adjust short delta" banner, rejected-pairs list. Needs its own rendered mock (Dean cannot review ASCII) and Dane's UI check first (Mock 3 assumed a surface that does not exist).
5. **EARNINGS-NO-DATE-0001** (draft; missing-earnings-date caution; sequenced after D, which is done): needs Paul's scope and Diane's tag copy.
6. **PMCC registry migration** (SCREENER-CONFIG-0001 phase after CC): prerequisites met. It absorbs the PMCC scan receipt component, the dropped "always on" debit row and the F1 receipt row.
7. Small follow-ups: held card nested-button accessibility (Refresh Portfolio sits inside the card's expand button; split the header toggle), `pmccHeldReadinessClient` labels every blocked held pair `quote-quality`, `pmccScore.legLiquidityFraction` review (C1 fixed the held-long null-OI case), inline error text for invalid PMCC delta (Run is silently disabled).
8. **EM-AMBER-0001 (parked until data exists; owner Ian and Alan):** decide whether to add an amber highlight for a short strike that is well inside the expected move. Do NOT pick a number by intuition (0.70x was rejected as unprincipled). Trigger: about 20-30 new spread entries recorded after 2026-09-25 (the snapshot data fix). Then run the console query in `docs/analysis/entry-distribution-console.md`, split by index/ETF vs stock, and set the amber where a candidate is more aggressive than what Dean actually enters; check the Performance page's inside/outside split for outcomes. Ambient color only, never a check status, gate or state. Also on this item, each only if Dean misses it: an opt-in scan control "Outside expected move" (default off, live count), "find next best outside EM" (Find Better, show the delta and credit change, Back to original) and Ian's "next strike out" hint showing credit and ROC cost; a compound rule (well inside the expected move and above the 50-day average; the app has MA20/50/200, no 30-day); a gradual Ranked EM score instead of the cliff at 0% (delta 0.12 and 0.16 score the same), using a multiple-of-EM measure; and whether the delta bands themselves should move (methodology, not UI). Background: `docs/tickets/EM-CONTEXT-0001-expected-move-is-information-only.md`.
9. **POSITION-INTENT-0001 (STEP 1 AND THE POSITIONS-VIEW CONTROL DONE 2026-09-25: the intent dropdown was found missing from the new Positions workspace (it only existed on the old position cards, hidden when the workspace is on); `lib/positionIntent/vocabulary.ts` (strategy-specific words), `/api/position-intent` accepts the new values, `IntentSelect` on every Position Analysis row and every Portfolio instrument card, Wheel = PREFER assignment weight and treated like Acquire in the recommendation (Dean: an Acquire or Wheel CSP gets no strong action). REVIEW CARD DONE 2026-09-25: the Hold / PMCC / Undecided control replaces the Intended use dropdown, and the dashboard notes follow the plan (extrinsic judged against the user's own cap; breakeven amber only above 10%; IVR and the PMCC start note only for a PMCC plan, always grey, 2% start tolerance; no scoring change). STILL TO DO: the fill hand-off (Quinn first), an Intent column if wanted; the advisor pick cards still use the old callouts (no plan there). OPEN, needs Dean and Ian: every lone short put DEFAULTS to Acquire, and Acquire makes the legacy recommendation return Hold and skip ALL breach, stop and loss exits (`lib/portfolio-data/acquisition.ts`); the newer engine (`managementIntent.ts`) can still say Cut Losses at a 100% loss even with assignment preferred, so check which one drives the on-screen text) (approved by Dean 2026-09-25; build first; Ian approved with changes, Quinn pass on the fill hand-off before that step):** one intent per position, in one store: the EXISTING `/api/position-intent` (Redis, keyed by `position.key`; the Portfolio page already has an "Intent: Income / Acquire / Neutral" dropdown, defaults CSP to Acquire and spreads to Income, and feeds the AI prompt and the assignment preference). No second capture in the LEAP mandate. Each strategy uses its own words and a value never appears on another strategy: LEAP = Hold / PMCC / Undecided (replaces the misfit Income/Acquire/Neutral on LEAP cards; old stored values on a LEAP fall back to Undecided); CSP and CC = Income / Acquire / Wheel; BPS, BCS, IC = Income (Neutral kept). Wheel maps to `positionStrategy` WHEEL with assignment preference Prefer (same weight Acquire has today); Income and Acquire keep behaving exactly as now (leave `positionStrategy` unset for them); the 21-DTE stop, 2x credit loss stop and material-loss Cut Losses are unchanged. Build order: (1) store and API vocabulary; (2) Portfolio position cards and a new Intent column (strategy-specific words, reads the one store; each chip opens that position's card; the card starts with one identity line: contract, DTE, cost, P/L; PMCC intent shows the short-call summary with credit in per share and total dollars and a "View cycle" link, or the existing "Review PMCC short calls" button; income rules stay reachable while a short call is open; Undecided with a short call open suggests PMCC in grey and never sets it); (3) the LEAP review card control (Hold / PMCC / Undecided, one "Intent not set" line, no note boxes when Undecided) and the intent-aware callouts (extrinsic vs the user's own cap, breakeven amber only above 10%, IVR notes by intent and grey for PMCC, PMCC start note for PMCC intent only with a 2% tolerance; no scoring, qualification or ranking change; IVR is not added to the score); (4) the fill-time hand-off from review to store, last, after Quinn confirms the fill produces the same `position.key`; if it cannot be matched the position saves as Undecided, never a guess. Intent is context only and never gates an order. Layout mock: https://claude.ai/artifact/BKzx536MusexAkgKK7LMnL (LEAP cards, CSP card, Intent column). Dean also wants Wheel in this ticket, not deferred.
10. **RSI-TURN-0001 (PHASE 1 DONE 2026-09-25: `lib/indicators/rsi.ts` and `rsiTurn.ts` with Alan's fixtures, `RsiStrip` in the shared quick-chart popup (Positions table, Portfolio tab, Screener), and an `RsiLine` under the spread and CSP order windows; STILL TO DO: the popups on Rinse-Repeat, Engine and the older portfolio card (last, fixed anchoring spec), the other order windows (covered call, PMCC, LEAPS if wanted), and the live-bar timestamp fix) (approved in principle by Dean 2026-09-25; second after POSITION-INTENT-0001; Alan validates the formula with golden fixtures before build; information only, no gate):** the app only has a single RSI number today (`lib/screener/trend.ts`), so it cannot see a turn. Compute RSI(14) with Wilder smoothing from the daily closes the chart popups already fetch (about 90 bars, no extra call), and classify the last bars as turning up (was at or below the low line within 5 to 10 bars and now lifting 3 to 5 points with two consecutive higher readings or a cross back above it), turning down (mirror from 70), still falling, or neutral. All thresholds are provisional; tune against Dean's own Trade Log entries before trusting them. Show it as a label plus a small RSI line with 30 and 70 levels and a plain caption, in EVERY chart popup (shared `ChartLinkButton` first, which covers both Positions tables; then the Screener, Rinse-Repeat, Engine, and the older portfolio card popup last because its fixed anchoring spec has regressed before) and as one neutral line under "Expected move (1 SD)" in the order windows. NOT in the scan result cards yet (would need about 90 bars per candidate; revisit once the cost and use are known) and not on the Positions or Portfolio tables. Never a pass or fail, amber or a ranking input; any use in scoring needs Ian. Mock: https://claude.ai/artifact/EvGL7BG8cUtYsJZdqApuhF. **Alan's validated spec (2026-09-25, approve with changes; all thresholds PROVISIONAL, calibrate from Trade Log entries at 30 to 40 per strategy):** Wilder seed = mean of the first 14 gains and losses (15 closes minimum), then AG = (AG*13+g)/14; flat series = 50, no losses = 100, no gains = 0; fail closed (return null, never repair) on non-array, too few, NaN, null, non-finite or non-positive closes; never round inside the function. 90 closes is enough (seed influence 0.39%; simulated last-bar difference at most 0.15 RSI points). State on the last N=8 RSI values (m = min, M = max, latest inclusive), first match wins: TURNING_UP if m <= 30 and R >= m+3 and R < 50 and two consecutive rises; TURNING_DOWN if M >= 70 and R <= M-3 and R > 50 and two consecutive falls; OVERSOLD_NO_TURN if m <= 30 and R < m+3; OVERBOUGHT_NO_TURN if M >= 70 and R > M-3; else NEUTRAL; fewer than 8 values or a non-finite value in the window = INSUFFICIENT. "Cross back above 30" is dropped as confirmation (noise). Split `rsiSeries(closes)` from `rsiState(rsiArray)` so boundary tests run without prices. Ian-lens decisions (inline): keep 30/70 and lift 3 with two rises; use completed daily bars only and show the last bar's date (needs bar timestamps, which the popups do not keep today: OPEN, first version labels the latest bar as possibly live); no smoothing of the oversold/neutral flicker until data shows it matters. Copy says "entry timing hint", never "reversal".
11. **CUT-LOSSES-REVIEW-0001 (approved by Dean 2026-09-25; AFTER DECIDE-0001 (Dean, 2026-09-25); Ian: approve capture and presentation, reject any scoring change until data exists; Paul: DECIDE-0001 owns the calm presentation, the Acquire and Wheel logic and the log, so this ticket shrinks to the MAX_LOSS label question and the close-versus-expiry price capture):** Dean reports Cut Losses "blaring" led to panic closes on positions that later recovered. Evidence so far (Trade Log CSV, 65 closed trades, Apr 29 to Sep 21 2026, net +$2,426): CSP 13 trades net +$2,957, spreads (BPS and BCS) 52 trades net -$530; 16 of 28 losses were closed with 21+ DTE left (-$3,543, 54% of loss dollars); 21 of 28 losses closed before reaching 2x credit, so the 100-point material-loss trigger is not the only cause; only one position was held near expiry (NVDA CSP, closed 2 days before expiry at +49.8%, tagged HELD_TO_EXPIRY; confirm the tag and the record). Decision Outcome Tracking (`lib/decision-review`) holds 0 reviews, so the rule's hit rate cannot be measured yet. Scope: (a) soften the presentation (calm "Loss stop reached, review" instead of a red command) and show DTE, distance from the strike, the position's intent and what it needs to recover; (b) separate CSP from spreads in the recommendation logic (a CSP set to Acquire or Wheel should not get a bare Cut Losses from a mark-to-market loss; spreads keep the 2x credit stop; needs Ian's approval as a risk-logic change); (c) start capturing the missing data: each time Cut Losses shows (symbol, DTE, strike distance, P/L %, intent), the underlying price at close and at expiry, and what Dean did next. Trade Log columns Score, Profit Target, Stop Loss, Entry State and Entry Overrides are empty on every row because entry snapshots only started 2026-09-25. Also check: MAX_LOSS labels trades that lost 15% to 27% of credit; find out what the label means. No change to scoring weights until the outcome data exists (about 2 to 3 months).

12. **DECIDE-0001 (calm, decisive position recommendations; ticket `docs/tickets/DECIDE-0001-decisive-position-recommendations.md` REVISION 7 (2026-09-26; Dane's developer review folded in: only S3-0a, the false-"Hold" fix, can start, after Dean's decision 7 and two small answers); rendered mock revision 6 https://claude.ai/artifact/1mhkVcQUysMBL2vPvrmucA; team review record `docs/tickets/DECIDE-0001-review-2026-09-25.md`):** every lens has reviewed the design (Ian, Alan, Quinn, Paul, Diane) and all required changes are folded into revision 6. Paul (G4) approved Phase 0, 0001A and the display-only safety slice S3-0; everything else slice by slice. Slice plan: S0 data prerequisites (a NEW history endpoint; do not change `GET /api/chart`), S1 signals display only, S3-0 safety and no-red (no false Hold, contradicting banners deleted, red removed from existing labels; independent of S0 to S2), S3-1 bought options and `needsClose` (live logic, Ian first), S2 shadow evaluator (disagreement list via a console script Ian approves), S3-A one source (card AND the priority/briefing engine call), S3-B the calm cell, S3-C card/panel/AI prompt, S3-D priorities and dashboards, later (uncovered and PMCC short calls, covered-call net line, roll cost, sign-in expiry, fallback stop, states O, P, P2), S4 log; 0001C (AI explanation, new route, Paul's D2 row, Ian's D9 lexicon change) and 0001E (Finnhub) are separate tickets. Key findings: the live engine is `scorePortfolioPositionObjective` (a second call feeds the priorities); `getRecommendation` is dead code; a missing recommendation shows "Hold" today; bought options are classified by label, not direction; the stop today needs a TradeEdge stop order. DEAN OWES seven decisions (listed at the top of the ticket): approve the mock, approve the dropped behaviors (including labels on bought options), the broken-stock gate (Ian and Alan recommend a loss of at least half the credit or delta 0.30), the fallback stop (Paul: defer), the no-stop-order case, the covered-call net line, and go-ahead on S3-0. Then Dane's (developer) pre-development review (G6), which is in progress.

13. **KEEP-CREDIT-0001 (draft 2, 2026-09-26; ticket `docs/tickets/KEEP-CREDIT-0001-keep-the-credit-odds.md`; Dean's biggest problem: closing spreads that would have expired worthless):** Ian, Alan, Quinn, Paul and Diane reviews folded in; Dane's developer review done (B1 can start after S3-0a; A1 needs a fee model and roll-math decisions; A2/B3 blocked on DECIDE S3-B; B2 blocked on the label mapping). Part A (first-time-roll explanation in the batch dialog's Close+Roll cards and the roll suggestion fold-out; shipped alone first). Part B (odds line only on "Hold with a loss": spread or Income put, loss >= 25% of credit, above the stop, more than 21 DTE; fold-out only on the time-limit state; never on stop, max-loss, Acquire/Wheel, bought or expires-soon states; rounded to 5%; no touch percentage in v1). Slices A1, A2, B1, B2, B3. Follow-ups: KEEP-CREDIT-0002 (hindsight review of closed spreads; needs 100+ trades and 30+ per bucket) and KEEP-CREDIT-0003 (parked). Four wrong "touch" comments to fix (`positionMetrics.ts:412-418`, `PositionsWorkspace.tsx:726`, `acquisition.ts:2116-2121`, `types.ts:156-158`). Follows DECIDE-0001 S3-0. Rendered mock published for Dean.
14. **WHEEL-SYSTEM-0001 (concept and mock only, 2026-09-26; nothing built; Dean's idea: one wheel-native system that picks the next wheel candidate and manages capital efficiently; supersedes the parked Wheel page and wheel simulator if built):** Ian and Paul framed it after reading the Wheel page, the wheel simulator and the Income Engine. Verdict: neither scrap nor adapt as is; keep the live plumbing (chain, IV rank, earnings, trend/RSI, real positions and share basis, Wheel intent, Income Engine capital bars), drop the always-wins simple simulation, the greedy-by-yield allocator, the hardcoded Engine suggestions and browser-only settings. Four slices in order: W1 capital plan (profile-based per-name limit, reserve, sector limit, unlock table, stress test), W2 candidate ranking, W3 cycle ledger with true cost basis (hardest: needs assignment linkage from trade history), W4 idle-capital view. Mock: https://claude.ai/artifact/PqJWm8PT2WvR4SwZHwDmTH. **Screen 1 (capital plan) approved by Dean 2026-09-26** ($50,000 account; profiles Careful 6%, Balanced 9% (recommended), Concentrated 12% of the account lost if one name falls 30%, giving a highest put strike of $100, $150, $200). UPDATE 2026-09-26 (real prices, Yahoo last close 9/25, verify in broker): at $50k only sector ETFs fit a cash-secured-put wheel (XLU, XLF, XLE, XLP always; XLV and PLTR only under Concentrated); MU (~$1,082), AMD (~$631), META, MSFT, SOXX and the rest of the Mag 7 do not fit any profile (one put needs $20k to $100k of cash). The first mock used invented, too-low prices and was corrected. Dean chose: small ETF wheel now, spreads on names he likes, grow into them via an unlock ladder (cash / 0.30 = account size at which a name fits Balanced). Dean 2026-09-26: the Income Engine's SPX bucket is retired (SPX spreads are 10x the size of SPY, so one loss is too large; SPY, VOO, QQQ, DIA do not fit a put wheel and are not part of this plan; XSP mini-SPX noted as an option only); no bucket for them. W1 TICKET DRAFT 1 written (`docs/tickets/WHEEL-SYSTEM-0001-capital-plan-and-ladder.md`): $45,000 to the wheel, $5,000 reserve, Balanced, ETF start, 10%/yr premium hurdle at the bid after fees (used from W2), spread risk cap proposed at 5% ($2,500). DRAFT 2: Alan, Quinn, Dane reviews folded in (fixtures verified by script; integer basis-point math, failure states, strike rule, schema). Dean ACCEPTED (2026-09-26) the $5,000 spread cap / $40,000 wheel / $5,000 reserve split (worst case about 30% of the account: wheel -25% plus spreads at full loss; single spread max 2% = $1,000; review after about 10 spreads, back to $2,500 if panic-closing returns). Ticket is DRAFT 3, ready for Dean's approval to build W1. STAGED PATH (Dean, 2026-09-26): stage 1 = learn the cycle on sector ETFs (plan on 1 to 1.5% a month, 2% a good month; theoretical put premium at 30 days needs about 35% implied volatility for 2% a month at 0.30 delta, so ETFs at 15 to 30% IV top out near 1 to 1.8%), stage 2 = higher-volatility names as capital reaches their unlock size. Graduation milestones (Ian): a few full cycles including one assignment and one call-away with rules followed, plus account at the unlock size, plus earnings checked. The Plan tab shows the next unlock. Dean adds no new capital, only reinvested returns: on compounding alone XLV unlocks in about 4 to 8 months, PLTR 1 to 2 years, NVDA 2 to 4 years, AMZN 3 to 5 years (no losses, fees or taxes); levers are the Concentrated profile (thresholds x 0.75), spreads, or LEAPs. STILL NEEDED: Dean's criteria for a stock worth wheeling (drives W2), the loss he would not accept in a broad selloff, sector limit and reserve confirmation, whether the Wheel page, simulator and the Engine wheel bucket are replaced. Ian signs off the score; Alan the arithmetic and fixtures; Quinn the cycle ledger; Diane the mock. Known problems found in the old code (if kept): the simulator values an unassigned put at its current strike (suspected phantom gain, unconfirmed), never uses cost basis, and drifts at trailing growth; the Wheel page has no below-basis call guard, no earnings, IV rank or liquidity filter.

## Later (on the horizon, not yet scoped)

- PMCC-HELD-LONG-FLAGGED-0001: draft, not approved. Held LEAPs that go OTM are dropped silently (`LONG_NOT_ITM`); needs a rendered flagged-state mock. Approve after 0001C's surface exists.
- Paper trading parity, six phases: (1) stock + CC, (2) PMCC, (3) LEAP-only, (4) rolls, (5) GTC/stop simulation via Vercel Cron, (6) assignment/expiration. Plan lives in `docs/paper-trading-full-parity-plan.md`. No phase ticketed or scoped yet.
- PMCC three-stage discovery flow: partially implemented. `isPairedPmccLong` in `lib/portfolio-data/pmccPairDetection.ts` is reusable. Blocks AI-POLICY-0001E.
- Task manager / background scan track (TE-0001 through TE-0005D, RF-0001): tickets read "Ready for Implementation" but reference `feature/autopilot-paper-mode`, which no longer exists. Status unverified; reconcile against main before scoping.
- LCC-0001A through E (equity-aware LEAPS/CC/PMCC lifecycle): the epic is "Approved for technical specification" and B through E read "Ready after" their predecessor. A is merged; B-E build status unverified.
- AI-POLICY-0001 (B, C, D, E, F): drafts. C is blocked on Diane's mocks, E on the PMCC three-stage flow landing, and F blocks enabling any route in Production.

## Process notes (learned 2026-09-24)

- Diane's mocks for Dean must be rendered (HTML artifact or screenshot); Dean cannot review ASCII. Where he delegates to Ian, record it in the mock doc.
- Before merging code to main run a real `next build` (es5 target rejects `for...of` over Maps; tsconfig.check.json and vitest do not catch it) and confirm Vercel's production status on the exact commit afterward. Merge from a scratch worktree so uncommitted local work is untouched. No full suite after a merge when the branch already ran it and main did not move.
- Agents read files from origin/main (`git show origin/main:<path>`) because the local checkout is stale while Dean has uncommitted work in `page.tsx`.

## Parked / deferred (with why, and what would unstick it)

- Rinse & Repeat OTOCO stop proof and the wheel / wheel-simulator features: parked by Dean 2026-09-25 (not needed yet). Unstick: Dean decides to use Rinse & Repeat live (then: dry-run proof of the multi-leg stop shape per ES-0003, then the Stop Limit ticket).
- SCAN-ALIGN-0001F2: DONE (moved out of Parked; merged `a34098d`).
- LEAPS-ADVISOR-0001B: scoped, not built; no ticket file in docs/tickets/. Unstick: team go-ahead, or a re-scope decision. Gates F2.
- PMCC-ASSIGNMENT-RISK-0001: draft. Short-to-LEAP expiry-gap investigation goes now. Ex-dividend part is deferred. Unstick: Dean's ex-dividend data-source decision.
- After-hours CC "not ready" chip: rides with C as a display state only, held. Unstick: Ian's confirmation (his open item 2, management rules and "not ready").
- Autopilot extension to PMCC/CC: undecided product question, Dean's open decision. Current scope is BPS/BCS/IC/CSP only. Do not propose implementation.
- SCREENER-VISUAL-0001: draft, deferred by Dean 2026-09-23. Unstick: pick up alongside the CC, PMCC and LEAPS phases of SCREENER-CONFIG-0001.
- ENTRY-0002: deferred. Unstick: a reviewable complete-snapshot cohort of closed credit spreads.
- AI-POLICY-0001G: deferred, do not start. Unstick: Paul defines the reports (D10).
- STOCKS-ORDERS-0001: proposed, not built, touches the order path. Unstick: Diane mocks the actions column and dialog, then Alan and Ian review.
- PERFORMANCE-STRATEGY-0002: draft, not approved. Unstick: approval.
- TRADELOG-0002: proposed. Unstick: approval.
- ENTRY-0001 release enablement: implemented, awaits Quinn's live filled-OTO verification.
- Out of scope for SCAN-ALIGN-0001: roll/exit recommendations, ex-dividend on covered calls, missing earnings date as a hard exclusion, `pmccStartPrice` changes beyond the label and held-mode input.

## Dead (do not re-propose, with why)

- FUNDAMENTALS-0002: reverted at 762da02. Finviz pre-screen replaces it. Any future fundamentals gate needs a new data-source decision first.
- `feature/autopilot-paper-mode` branch: no longer exists. Autopilot code is already on main.
- Filter scan mode: standing decision. Rank and Targeted only (SCREENER-CONFIG-0001-filter-mode-decision, decided and implemented 2026-09-21).

Done (do not re-propose): screener results, CSP workflow, PM-0001/0002 position metrics, screener job progress, LCC-0001A, TT token expiry fix, recommendation/actions split, stop-dialog labeling, net-edge DTE dampen. Also implemented per ticket Status: SCREENER-CONFIG-0001A/B, SCREENER-SORT-0001, CSP-IVR-0001, CI-0001, AI-SEC-0001, STOCKS-0001, PNL-BASIS-0001, TE-0007A, and the LEAPS-DASH/POS/SINCE/ENTRY/CYCLES/SPARK/LEDGER/MANDATE/EVENTS/AI-0002/AI-0003/PMCC-START series.

## Tickets with unverified status

Paul could not read a Status line for these, or the status ("Ready for ...") cannot be reconciled with main. Reconcile before relying on them.

- No Status line found: SCREENER-OI-0001, SCREENER-UX-0001, SCREENER-PREFS-0001, SCREENER-LIQUIDITY-0001, SCREENER-CONFIG-0001 (unified scan configuration), CSP-0002, CSP-WORKFLOW-0001 (reads "Partially implemented", remainder unclear), CSP-ORDERS-0001 (approved 2026-09-22, build status unknown), PORTFOLIO-MODE-0001, TE-0001, TE-0002, TE-0007, TE-0007C (implemented pure logic; screener wiring status truncated), SEC-0001, AI-POLICY-0001 (deterministic-first AI analysis), AI-POLICY-0001A (approved to start; build state unknown), ENTRY-0001A, PERFORMANCE-STRATEGY-0001, PMCC-0001 (both files), PMCC-0002, LEAPS-INCOME-READINESS-0001, LEAPS-PI-0001 (plan only), LCC-0001 execution-sequence and review-index.
- "Ready for implementation" or "Ready for validation", build state unverified: CSP-0003, IVX-0001, POSITIONS-0002, POSITIONS-0003, POSITIONS-0004, POSITIONS-0005, LCC-0001B/C/D/E, TE-0003, TE-0004, TE-0005A/B/C/D, TE-0006A/B, RF-0001.
