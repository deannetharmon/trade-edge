# TradeEdge — Session Handoff

Last updated: 2026-07-17 (PT-0001 Manual Paper Trading Sandbox — implementation complete on `feature/manual-paper-trading`, **awaiting Product Owner review, not yet committed/pushed/merged**). Paste this file (or point Claude at its repo path) at the start of a new chat to resume with full context.

## Governance

All future implementation planning, sprint management, repository management, and release decisions shall conform to:

planning/PROJECT_GOVERNANCE.md

If this handoff document conflicts with PROJECT_GOVERNANCE.md, the governance document takes precedence until intentionally amended.

## Project

Next.js/React options-trading dashboard (`options-screener`), deployed via Vercel. Repo: `/Users/deanharmon/Github/trade-edge` (this is Dean's selected/mounted folder — Claude reads and writes here directly).

Dean's standing preferences: full files not snippets (unless asked), very concise responses, check the project for context before asking questions.

## Current git state

- **IMPORTANT, discovered mid-session**: `feature/portfolio-intelligence` no longer exists (local or remote) — it was already merged into `main` (commit `a90f8f1`) and cleaned up by Dean between sessions, before this one started. `planning/PROJECT_GOVERNANCE.md` and `planning/SPRINT_STATUS.md` were also added the same day and were not part of this session's carried-over context. See this ticket's own report (`docs/reviews/PI-0014-Marketable-Pricing-Implementation-Report.md`, "Process Note") for the full account of what went wrong and how it was corrected.
- `main`: tracks `origin/main`, both at `c97a705` (OE-0001's merge commit), clean.
- PI-0014's branch history: originally committed directly to `main` (process failure, see "Process Note"), then moved to `feature/marketable-pricing` off `origin/main` and reviewed by the Product Owner (required a refactor — see the implementation report's Addendum), then unintentionally lost from all reachable refs during a `main` reset, recovered from dangling commits, corrected in a Corrective Closeout sprint (documentation drift, missing-data/invalid-quote test coverage, unknown-liquidity classification fix, `tsconfig.tsbuildinfo` cleanup), accepted by the Product Owner, and merged into `main` as `2c79d5e`. Full account in the implementation report's Process Note, Addendum, and Corrective Closeout Addendum.
- PI-0014's temporary branch (`feature/marketable-pricing`) has been deleted, locally and remotely, per the standard short-lived-branch lifecycle. It no longer exists.
- OE-0001's branch history: implemented on `feature/opportunity-engine-foundation` off `main` @ `a86c92d`, reviewed by the Product Owner (a corrective round was required — disposition-vs-disclosure ranking fix, final display-order fix, added component test coverage, documentation corrections), accepted, and merged into `main` as `c97a705`. Full account in `docs/reviews/OE-0001-Implementation-Report.md` (§10, Corrective Round Addendum).
- OE-0001's temporary branch (`feature/opportunity-engine-foundation`) has been deleted, locally and remotely, per the standard short-lived-branch lifecycle. It no longer exists.
- PT-0001's branch history: implemented on `feature/manual-paper-trading`, created off `main` @ `4812301`. **Implementation complete but not yet committed** — a stale `.git/index.lock` produced an `unable to unlink ... Operation not permitted` warning during a routine `git status` in this sandbox (the same recurring quirk noted below); per this sprint's explicit instruction, this was reported rather than forced/bypassed. Exact `git add`/`commit`/`push` commands (excluding the generated `tsconfig.tsbuildinfo`) are in `docs/reviews/PT-0001-Implementation-Report.md` §25 for Dean to run natively. Do not merge or delete this branch until reviewed.
- `feature/autopilot`: the one long-lived integration branch per governance — unrelated to PI-0014, OE-0001, or PT-0001, untouched throughout (still at `7e81cd1`).
- Local sandbox has no git network credentials, so pushes still need to happen from Dean's machine:
  ```
  git fetch
  git push origin feature/portfolio-intelligence
  ```
- **Known sandbox quirk**: `.git/index.lock` / `.git/HEAD.lock` can't be `unlink`ed (FUSE permission error) but CAN be renamed aside via Python immediately before any git command:
  ```python
  import os
  for f in ['.git/index.lock', '.git/HEAD.lock']:
      if os.path.exists(f):
          os.rename(f, f + '.bak' + str(os.getpid()))
  ```
  Harmless `tmp_obj_XXXX` unlink warnings during `git add`/`commit` are expected and non-fatal.
- **`npm run build` (`next build`) hangs at the initial Next.js banner in this sandbox** across multiple sessions/tickets — near-zero CPU, never completes. Treated as a sandbox/environment limitation, not a code defect, whenever `tsc --noEmit` is clean and tests pass. Recommend Vercel's build as the authoritative check. Don't burn time re-investigating this unless asked.
- Branches that exist: `backup/autopilot-before-main-merge`, `backup/portfolio-intelligence-before-sync`, `feature/autopilot`, `feature/autopilot-decision-engine`, `feature/portfolio-intelligence` (current), `main`, plus matching remotes, and `origin/feature/portfolio-lifecycle`, `origin/feature/watchlist-unification`. **`feature/autopilot-paper-mode` does NOT exist** — a ticket referenced it as a push target; flagged to Dean rather than silently substituted.

## Repo conventions learned this session

- Engineering-facing implementation reports live at `docs/reviews/<TICKET-ID>-Implementation-Report.md` (e.g. `PI-0006A-Implementation-Report.md`). Product-owner-facing reports are delivered as `.docx` via the `docx` skill when explicitly requested in that format.
- `docx` skill workflow: Node + `docx` npm package (not always preinstalled — `npm install docx --silent` if `require('docx')` fails), US Letter page size, `HeadingLevel`, `Table`/`TableRow`/`TableCell` need width set both on the table (`columnWidths`) and per-cell (`width`), both in DXA; `ShadingType.CLEAR` not `SOLID`; bullets via `numbering` config with `LevelFormat.BULLET` (never literal `•` chars). Verify rendering with `soffice.py --headless --convert-to pdf` → `pdftoppm -jpeg` → `Read` the resulting JPEGs before delivering.
- Tests: `vitest` (`npm test` = `vitest run`), colocated in `__tests__/` folders next to the module.
- `lib/portfolio-intelligence/` is the core rules/scoring engine for objectives, recommendations, priority scoring, health scoring, decision review, etc. — large, mature module built up over many tickets (see ticket log below).
- No existing "Max Loss" column matched the CSP risk-display request when it first came in — those files (`lib/calculateCspRisk.ts`, `components/CspRiskCell.tsx`) were built as standalone/portable code, not wired into a specific existing table cell, and Dean was told this explicitly.

## Ticket history (chronological, all complete unless noted)

Long run of Portfolio Intelligence (`PI-00xx`) tickets, each following the same loop — explore → implement → test → validate (`vitest`, `tsc --noEmit`, attempt `next build`) → commit — all on `feature/portfolio-intelligence`:

- **PI-0006A** — Assertive Recommendation Engine (decisive labels: "Exit Position", "Take Profit", etc., replacing generic "Watch"/"Roll Soon"/"Manage position").
- **PI-0006B** — Management Intent engine (`ManagementIntent` type + selection logic, net-edge decline evidence).
- Decision Scorecard added to Position Intelligence panel.
- **Remaining Opportunity Engine** (`lib/portfolio-intelligence/remainingOpportunity.ts`).
- **Decision Quality Matrix** — centralized scoring weights refactor.
- **Decision Review / Outcome Tracking** — persistence layer, Decision History subpage, "Needs Follow-Up" view.
- **Trade Log reconstruction** — shared `lib/tradeLog/reconstructTrades.ts` used by both trade-log and performance pages.
- **PI-0009A** — Position lifecycle snapshots (Redis-backed API route + capture wiring).
- **PI-0009B** — Outcome analysis wired into Decision History.
- **PI-0010A** — Today's Priorities dashboard (`buildTodaysPrioritiesDashboard`, new default portfolio tab).
- **PI-0010B** — Intelligent Prioritization (`lib/priorityScore`, `PriorityRankedList` component).
- **PI-0011A** — Mission Control tab (`MissionControl.tsx`).
- **PI-0011B** — Portfolio Health Engine (`lib/portfolioHealth`, health score section in Mission Control).

Then, separately (not part of the PI-00xx sequence):

- **CSP Max Loss / risk display** — added `lib/calculateCspRisk.ts` + `components/CspRiskCell.tsx` showing a 2-sigma volatility-based loss estimate ("Realistic Loss" at the time) alongside the existing "Capital at Risk" (stock-to-$0 theoretical worst case). Delivered with a product-owner-facing `.docx` implementation report (`docs/reviews/CSP-Realistic-Loss-Implementation-Report.docx`, still in repo, now superseded — see below).
- **CSP terminology refinement (commit `e35c96d`)** — renamed "Realistic Loss" → **"2σ Scenario Loss"** everywhere (field name `scenarioLoss`, was `realisticLoss`; `expectedMove`, was `twoSigmaMove`) because the old label implied more statistical certainty than the calculation provides. Kept the Capital at Risk calculation and all CSP math completely unchanged except one narrow defensive fix: `daysToExpiration` is now clamped to ≥0 before `Math.sqrt()` to prevent `NaN` on invalid/negative DTE input (doesn't change output for any valid DTE). Added a visible "2σ Scenario Loss" label in the UI (previously the primary value had no visible text label) and rewrote the tooltip to describe it as a modeled scenario, not a prediction. Added 8 new targeted tests (`lib/__tests__/calculateCspRisk.test.ts`) covering breakeven, both IV input formats (whole-percent vs. decimal), the $0 floor, DTE=0, and the negative-DTE regression. Report: `docs/reviews/CSP-2Sigma-Scenario-Loss-Terminology-Refinement.docx`.
- **Portfolio Intelligence UX Polish (commit `2ee4b0e`)** — readability/layout refactor of the expanded Position Intelligence panel, no scoring/calculation/recommendation-logic/API changes. Added a `SuggestedActionCard` at the top of `PositionIntelligencePanel.tsx` (urgency-colored, elevates label/suggested action/confidence/confidence-tier/top evidence/remaining-opportunity — all pre-existing values, nothing newly computed); restructured the rest of the panel into a responsive `grid-cols-1 lg:grid-cols-2` layout (narrative left, reference/next-steps right); hid the existing Decision Scorecard and Decision Review sections behind `SHOW_DECISION_SCORECARD`/`SHOW_DECISION_REVIEW` flags (both `false` — logic/persistence untouched, one-line flip to restore, but their in-panel expand/collapse tests were removed since they're unreachable while hidden); added a "RULE ENGINE'S EXISTING CALL" block to `buildPositionPrompt()` in `app/portfolio/page.tsx` instructing the AI Analysis prompt not to restate the rule engine's existing recommendation verbatim (prompt copy only, JSON schema/API unchanged). Report: `docs/reviews/Portfolio-Intelligence-UX-Polish-Implementation-Report.md`.
- **PI-0012 — Portfolio Review Architecture (design only, commit `fcaebce`)** — a design document (`docs/design/PI-0012-Portfolio-Review-Architecture.md`) inventorying every existing Portfolio Intelligence engine (Health Score, canonical objectives, Priority Score, Decision Review/Outcome Analysis, Trade Log) and proposing a thin `lib/portfolioReview/` composition layer for a new "Portfolio Review" capability, rather than a second scoring engine. Identified two genuinely new (but purely additive) aggregations needed — trailing performance rollup and portfolio-level decision-quality rollup — and recommended NOT adding a new composite score. Phased as PI-0012A (composition only) through PI-0012D (window selector polish). No application code changed for this ticket.
- **PI-0012A — Portfolio Review Composition Layer (commit `69b64c3`, pushed)** — implements Phase 1 of the PI-0012 design. New `lib/portfolioReview/` package (`buildPortfolioReview()`, pure, no fetch/API/React) composes the existing Portfolio Health Score, canonical portfolio-level objectives (concentration/buying-power/idle-cash/income, filtered not re-evaluated), and Today's Priorities' already-scored objectives (Top Risks, sorted not re-ranked) into one `PortfolioReviewSnapshot`, plus a new Portfolio Composition aggregation (position count, strategy counts, symbol concentration via the existing `derivePositionConcentration()` helper, Wheel-managed fraction via the existing `deriveWheelDominance()` helper). Rendered via a new `<PortfolioReviewCard>` placed as the first section on the Portfolio page's Positions tab, above the position list — deliberately not a new tab. No new score, no new ranking, no new recommendation logic, no persistence, no AI. Trailing performance and Decision Quality rollups are explicitly deferred to PI-0012B/C. Report: `docs/reviews/PI-0012A-Portfolio-Review-Composition-Implementation-Report.md`. A product-owner-facing summary was also produced: `docs/reviews/PI-0012A-Portfolio-Review-Implementation-Summary.docx`.
- **PI-0013 — Daily Briefing Dashboard** — a new "Today's Briefing" card, first on the Portfolio page (above Portfolio Review). New `lib/dailyBriefing/` package (`buildDailyBriefing()`, pure, no fetch/API/React/AI) composes Portfolio Review's `PortfolioReviewSnapshot` (Health, Top Risks, concentration/capital concerns — all reused verbatim) and Today's Priorities' dashboard (DTE/earnings/follow-up buckets for Upcoming Events, opportunity buckets for Opportunity Summary, Immediate Action for Risk Summary) into one `DailyBriefing` model: a deterministic (no AI/LLM) template-generated Executive Summary sentence, Today's Priorities (Portfolio Review's own Top Risks, unchanged), a Portfolio Snapshot stat grid, Upcoming Events, Opportunity Summary, and a five-category Risk Summary (concentration, capital, assignment exposure via the existing `OBJ-ASSIGNMENT-RISK` ruleId tag, earnings exposure, immediate attention). `averagePositionHealth` was lifted out of the page's existing `healthInput` computation into its own single-source `useMemo` so both Portfolio Health and this new card read the same value rather than each computing it separately. No new score, no new ranking, no new recommendation logic, no persistence, no AI. Report: `docs/reviews/PI-0013-Daily-Briefing-Implementation-Report.md`.
- **PI-0014 — Marketable Pricing for Risk-Gating, Phase 1 (complete, merged into `main` at commit `2c79d5e`)** — every risk-gating decision (stop-loss, take-profit, emergency exit, Cut Losses) previously read only mid-market P&L (`currentValue`/`pnl`), never the marketable "if I closed right now" price (`closeValue`/`closeNowPnl`) that already existed and was already correctly computed — confirmed against a real position (mid P/L −$129 vs. marketable P/L −$624 against a $644 max risk). Two rounds of independent external architecture review (ChatGPT, Gemini) were reconciled into a final ruling (`TradeEdge_Final_Architecture_Rulings.md`), treated as the implementation contract. New `lib/positionValuation/` package (`computePositionValuation()`, pure, no fetch/API/React, the only export) produces a `PositionValuation` object: mid/marketable value and P&L, slippage cost, and a `liquidityTier` (`LIQUID`/`WIDE_SPREAD`/`LIQUIDITY_TRAP`, fixed thresholds against max risk, or `null`/unknown when `maxRisk` is missing, zero, or negative — corrective closeout fix, never defaults to `'LIQUID'` on missing risk data). The canonical Decision Engine (`evaluatePositionObjective()` in `lib/portfolio-intelligence`) accepts optional `marketablePnlPct`/`liquidityTier` inputs, widens `materialLoss`/`weakHealthLoss` to fire on either mid or marketable evidence (Cut Losses can only get more conservative, never less), vetoes `profitTargetReached` when marketable data contradicts a mid-based profit claim, and owns `executionRealityPromoted`/`liquidityTrapTriggered` (the latter true only when tier is `LIQUIDITY_TRAP` AND marketable evidence actually changed the recommendation — tier and trigger are deliberately independent, and `liquidityTrapTriggered` lives here, not on `PositionValuation`, per a required Product Owner refactor). `app/portfolio/page.tsx`'s local `getRecommendation()`/`isActionRelevant()` stop-loss and emergency-exit checks were updated the same way (OR with mid); take-profit threshold branches there were deliberately left on mid pricing (documented, bounded scope). Health scoring, priority ranking, Portfolio Review, and Daily Briefing are unchanged, per the ruling's explicit Phase 1 boundary. Test coverage: the original permanent 5-fixture regression suite (real SMH-shaped failure, plain CSP, comfortable OTM spread, ITM/breached spread, highly liquid baseline), plus a corrective-closeout addition covering missing/omitted marketable data (mid-only fallback for all three gates), marketable evidence still gating when `liquidityTier` is unknown, and missing/zero/negative-`maxRisk` → null-tier classification. Design doc: `docs/design/PI-0014-Marketable-Pricing-Risk-Gating.md`. Report: `docs/reviews/PI-0014-Marketable-Pricing-Implementation-Report.md` (Process Note, Product Owner Addendum, and Corrective Closeout Addendum).
- **OE-0001 — Opportunity Engine Foundation (complete, merged into `main` at commit `c97a705`)** — the canonical Opportunity Engine foundation, answering "where should my next dollar go?" New `lib/opportunity-engine/` package: `evaluateOpportunityCandidate()` (per-candidate disposition: `RECOMMENDED`/`ACCEPTABLE_ALTERNATIVE`/`WATCH`/`REJECTED`, never recomputing Opportunity Score/Decision Confidence/Net Edge and never overriding an existing Decision Engine hard rejection) and `rankOpportunityCandidates()` (deterministic batch ranking, capital sequencing, and a two-pass final-display-order step that guarantees disposition precedence — `RECOMMENDED` always displays ahead of `ACCEPTABLE_ALTERNATIVE`/`WATCH`/`REJECTED`, etc.). One candidate adapter, `decisionAnalysisAdapter.ts`, normalizes an already-computed `DecisionAnalysis` into an `OpportunityCandidate`, compatible with the real shape `POST /api/autopilot/recommendations` already produces — though no production route calls it yet. A read-only `components/opportunity-engine/BestOpportunitiesPanel.tsx` was built and is **intentionally not mounted anywhere in production**: a first implementation round mounted it as an empty tab on the Income Engine (`app/engine/page.tsx`), which the Product Owner rejected as a production surface with nothing behind it; that mount was fully reverted. A Product Owner corrective round also fixed a defect where ordinary nonzero existing ticker/sector exposure was incorrectly treated as a disposition-demoting conflict (now split into disposition-changing `portfolioConflicts`, exact-duplicate-only, vs. informational-only `exposureDisclosures`), and added 15 new component tests plus a `vitest.config.ts` fix so tests under `components/` actually run. 697 tests passing repo-wide at merge; `tsc --noEmit` clean; local build hits the same documented sandbox limitation as every prior ticket. Vercel validation remains unverified. No paper or live execution exists. Design doc: `docs/design/OE-0001-Opportunity-Engine-Foundation.md`. Report: `docs/reviews/OE-0001-Implementation-Report.md` (§10, Corrective Round Addendum).
- **PT-0001 — Manual Paper Trading Sandbox (implementation complete on `feature/manual-paper-trading`, awaiting Product Owner review — NOT merged)** — a manual, intentional-action-only paper-trading sandbox for CSP/BPS/BCS/IC, structurally isolated from any live-order path. New `lib/paper-trading/` package: strategy/leg validation, marketable-fill pricing (bid/ask direction rules matching the existing `app/portfolio/page.tsx` `closeValue` convention, a stale-quote policy, and a Manual Paper Fill override), per-strategy capital/max-loss formulas, a pure accounting ledger (cash/reservedCapital/availableCapital/currentEquity/realizedPnl/unrealizedPnl/peakEquity), idempotency (scoped by user+operation+key, checked inside the same lock as the mutation), a dedicated Redis mutation lock (mirrors, does not share, the existing Autopilot run-lock pattern), and an append-only audit trail. Extends the existing canonical account record (`autopilot:paper-account:<userId>`) with one new optional field (`paperTrading`) rather than creating a second account per user — the still-dormant Autopilot Decision Engine's own paper framework is completely untouched. New paper-only API (`/api/paper-trading/*`), a new `/paper-trading` page and focused components (ticket, close, mark-refresh, reset, account summary, Portfolio Intelligence summary), linked from the home page nav. A Portfolio Intelligence adapter evaluates the paper portfolio using the exact same canonical `evaluatePositionObjective()`/`prioritizePortfolioObjectives()` real positions use — no forked logic. Two real bugs were found and fixed by this sprint's own test suite before ever being committed: (1) the pre-existing `/api/autopilot/paper-account` reset would have silently deleted a user's new PT-0001 ledger (fixed by having it preserve `paperTrading`); (2) a close-side pricing sign-convention bug that would have moved cash the wrong direction on every close (fixed in `pricing.ts`). 115/115 targeted tests passing; 812/812 tests passing repo-wide across 63 files; `tsc --noEmit` clean; local build hits the same documented sandbox limitation as every prior ticket. No live-order dependency anywhere (enforced by a source-scanning regression test, not just review). **Not yet committed** — see loose end #24. Design doc: `docs/design/PT-0001-Manual-Paper-Trading-Sandbox.md`. Report: `docs/reviews/PT-0001-Implementation-Report.md`.

## Loose ends / things a fresh session should know

1. **Push not yet done for the new PI-0013 commit** — sits directly on top of `69b64c3` (confirmed already on `origin/feature/portfolio-intelligence`), so `git fetch && git push origin feature/portfolio-intelligence` from Dean's machine should apply cleanly, no rebase expected.
2. **Old CSP docx still in repo, untracked**: `docs/reviews/CSP-Realistic-Loss-Implementation-Report.docx` is now superseded by `CSP-2Sigma-Scenario-Loss-Terminology-Refinement.docx` but was left in place (files in Dean's mounted folder can't be deleted without his explicit OK). If Dean wants it removed, ask first, then use the file-delete confirmation flow.
3. **`feature/autopilot-paper-mode` doesn't exist** — if a future ticket references it again, flag it the same way rather than assuming it should just be created or substituted silently.
4. **IV-normalization edge case, documented not fixed**: a decimal IV of exactly `1.0` (100%) gets misread by the `impliedVolatility >= 1` heuristic as whole-number-percent and divided down to 1%. Known, accepted, out of scope unless a real bug report comes in.
5. **`next build` validation is unreliable in this sandbox** — don't repeatedly retry it; note the limitation and move on, consistent with how PI-0006A, the CSP ticket, the UX Polish ticket, and PI-0012A all handled it.
6. **Decision Scorecard and Decision Review are hidden, not gone** — `SHOW_DECISION_SCORECARD`/`SHOW_DECISION_REVIEW` at the top of `PositionIntelligencePanel.tsx` are both `false`. If a future ticket asks to bring either back, flip the flag and restore/rewrite the removed expand/collapse tests (see the UX Polish report's "Trade-offs disclosed" section) rather than re-implementing the underlying components, which are untouched.
7. **No visual/screenshot QA on the UX Polish layout** — this sandbox can't render the live app (same build-hang issue), so the two-column grid and Suggested Action card have only been verified via passing component tests + code review, not an actual screenshot. Worth a manual look once deployed, especially the `lg` breakpoint collapse and all four urgency colors.
8. **PI-0012B/C not started** — Trailing Performance Rollup (win rate/P&L/by-strategy from `lib/tradeLog`) and Decision Quality Rollup (recommendation accuracy from `lib/decision-review`'s outcome analysis) are designed in `docs/design/PI-0012-Portfolio-Review-Architecture.md` but not implemented. Both are additive to `lib/portfolioReview/types.ts`'s `PortfolioReviewSnapshot` — no rework of PI-0012A's `currentState`/`composition` expected.
9. **No visual/screenshot QA on the Portfolio Review card** — same sandbox rendering limitation as the UX Polish sprint; verified via passing tests and code review only. Worth a manual look once deployed, especially the empty-portfolio state and the Wheel-managed stat reading "N/A" (expected — no live position carries a `positionStrategy` yet).
10. **Portfolio Review's Wheel-managed fraction will always read "N/A" in production today** — `positionStrategy` is passed as `null` for every real position on the page (no UI control exists yet to set it, same limitation already noted for `canonicalPriorities`). Not a bug; will start reading real values once that control exists.
11. **The untracked `docs/reviews/PI-0012A-Portfolio-Review-Implementation-Summary.docx`** was created after the PI-0012A commit and is not yet part of any commit — include it in a future commit if Dean wants it version-controlled.
12. **No visual/screenshot QA on the Daily Briefing card** — same sandbox rendering limitation as PI-0012A; verified via passing tests, `tsc --noEmit`, and code review only (including a markup-level, not rendered, check of the mobile-first grid classes). Worth a manual look once deployed, especially the six-stat Portfolio Snapshot grid at each breakpoint.
13. **Daily Briefing's Executive Summary is intentionally narrow** (health, immediate attention, concentration, earnings only) — see the PI-0013 report's Trade-offs section for what else could be added as simple additional sentence clauses without restructuring anything.
14. **PI-0012B/C are still not started** — unaffected by PI-0013; both remain purely additive to `lib/portfolioReview/types.ts`'s `PortfolioReviewSnapshot`, and PI-0013's `lib/dailyBriefing` would only need trivial changes (reading two more already-existing fields) to surface either once built.
15. **`lib/autopilot`'s own recommendation/risk-gate engines were NOT touched by PI-0014** — the final architecture ruling's Phase 1 contract names `app/portfolio/page.tsx`'s gates and the canonical `lib/portfolio-intelligence` Decision Engine specifically, not the separate, one-way-isolated `lib/autopilot` package. If autopilot's own gates read mid pricing the same way, that's a real, related gap worth checking, but it needs its own explicit decision before touching — flag it rather than assuming it's in scope.
16. **`PositionValuation` is computed and attached to every position (`pos.valuation`) but not yet rendered anywhere in the UI.** The data (mid/marketable P&L, slippage cost, liquidity tier, trap trigger) is available for a future slice to surface — e.g. a liquidity badge or an "if closed now" secondary P/L figure — without any new calculation. Deliberately deferred; PI-0014's scope was wiring the data into decisions, not display.
17. **No visual/screenshot QA on PI-0014** — same sandbox rendering limitation as every prior ticket. `tsc --noEmit` is clean and all 45 test files pass; nothing new is rendered in the UI this ticket (see #16), so the visual-QA risk is lower than prior UI-adding tickets, but still worth a manual look once deployed if `pos.valuation` gets surfaced later.
18. **Phases 2-5 of the marketable-pricing rollout are designed (in the two external reviews and the final ruling) but not started**: Phase 2 (full recommendation selector), Phase 3 (priority ranking), Phase 4 (Portfolio Review execution-risk rollup), Phase 5 (health scoring gains liquidity as an independent dimension). None require rework of PI-0014's `lib/positionValuation` shape.
19. **The Execution Reality Principle was discussed in the external review rounds but not yet added to `planning/DECISION_ENGINE_CONSTITUTION.md`** — a natural, low-risk follow-on if Dean wants the Constitution to formally reflect this ticket's approach.
20. **PI-0014 Corrective Closeout (this pass)**: fixed three real gaps found in a pre-merge architecture review of the recovered branch — (a) the design doc and this handoff still described the pre-refactor object shape (`liquidityTrapTriggered` inside `PositionValuation`, a nonexistent `attachLiquidityTrapTrigger()` export) after the Product Owner's required refactor moved that field to the Decision Engine; corrected both docs. (b) No test proved mid-only behavior was preserved when marketable data is absent/omitted, or that marketable P&L still gates correctly when `liquidityTier` is unknown; added coverage in `lib/portfolio-intelligence/__tests__/pi0014MarketablePricingFixtures.test.ts`. (c) Missing/zero/negative `maxRisk` silently classified as `'LIQUID'` (the best tier) instead of unknown; `computePositionValuation()`'s `liquidityTier` is now `LiquidityTier | null`, with `null` for an unusable denominator — existing 5%/15% thresholds unchanged when `maxRisk` is valid. Also removed an accidentally-committed `tsconfig.tsbuildinfo` diff from the branch. See the implementation report's Corrective Closeout Addendum for full validation results.
21. **This mounted folder rejects some `unlink`/remove-and-recreate filesystem operations from inside the sandbox** (confirmed multiple times across this project: a stale `.git/index.lock` that couldn't be removed — recurring again during both OE-0001 rounds — and a `git switch` that moved `HEAD` but left four tracked files' on-disk content unchanged because their old blobs couldn't be unlinked). In-place edits (open+truncate+write, what `Read`/`Write`/`Edit` tools do) are unaffected. Branch switches, `git restore`/`reset`/`checkout`/`clean`, and anything else requiring unlink-and-recreate should be treated as unreliable from inside the sandbox — verify with a diff/hash check after any such operation before trusting it, or have Dean run it from his native Terminal instead. Standing practice this project: stop and hand Dean the exact git commands rather than attempt to force past the lock.
22. **`BestOpportunitiesPanel` (OE-0001) exists, is fully tested, and is intentionally unmounted.** No page renders it. Wiring a real, live `DecisionAnalysis[]` feed into a page and mounting this panel against it is future, separately approved work — do not mount it with mock data, a new fetch, persistence, or cross-page state as a substitute if a future ticket asks for "the Opportunity Engine UI." See `docs/design/OE-0001-Opportunity-Engine-Foundation.md` §7 for the exact reason no page has a live feed yet.
23. **PI-0015 (Portfolio Intelligence corrections / live-market acceptance) remains queued, not started.**
24. **PT-0001 is implemented but not yet committed, pushed, or reviewed.** A stale `.git/index.lock` produced an `unable to unlink ... Operation not permitted` warning during a routine `git status` in this sandbox (see item #21 — the same recurring quirk). Per PT-0001's own explicit instruction, this was reported rather than forced/bypassed. Exact `git add`/`commit`/`push` commands are in `docs/reviews/PT-0001-Implementation-Report.md` §25 for Dean to run natively. **Do not stage `tsconfig.tsbuildinfo`** (a generated artifact, modified as a build side-effect, not part of PT-0001's actual changes).
25. **PT-0001 is the current active implementation sprint** as of this update — pending Product Owner review before any next sprint is selected.
26. **PT-0001's mark-to-market is manual-entry only** (no automatic browser-owned quote feed) — see its design doc §9 for why, and do not treat this as accidentally incomplete; it's a disclosed, deliberate Phase 1 boundary.

## Key files to know

- `lib/calculateCspRisk.ts` — CSP risk math (Capital at Risk + 2σ Scenario Loss).
- `components/CspRiskCell.tsx` — CSP risk table cell UI + tooltip.
- `lib/portfolio-intelligence/` — objectives, recommendations, priority scoring, health scoring, decision review engine.
- `features/portfolio/intelligence/PositionIntelligencePanel.tsx` — the expanded Position Intelligence panel (Suggested Action card, two-column layout, hidden Scorecard/Review sections).
- `lib/todaysPriorities/` — Today's Priorities dashboard orchestration.
- `lib/tradeLog/reconstructTrades.ts` — shared trade reconstruction logic.
- `app/portfolio/page.tsx` — main portfolio dashboard page, wires most of the above together (large file); also home to `buildPositionPrompt()` (AI Analysis prompt), `AnalysisPanel` (AI Analysis UI, separate from Position Intelligence), the `averagePositionHealth` single-source `useMemo`, and the `portfolioReviewInput`/`portfolioReview` and `dailyBriefingInput`/`dailyBriefing` `useMemo` pairs feeding `<PortfolioReviewCard>`/`<DailyBriefingCard>`.
- `lib/portfolioReview/` — Portfolio Review composition layer (PI-0012A): `buildPortfolioReview()`, `selectTopRisks()`. Pure, no fetch/API/React; composes `lib/portfolioHealth`, `lib/portfolio-intelligence`, and `lib/todaysPriorities` output, never recomputes any of it.
- `features/portfolio/review/PortfolioReviewCard.tsx` — the Portfolio Review UI card, second section on the Portfolio page's Positions tab (below Daily Briefing).
- `lib/dailyBriefing/` — Daily Briefing composition layer (PI-0013): `buildDailyBriefing()`. Pure, no fetch/API/React/AI; composes `lib/portfolioReview` and `lib/todaysPriorities` output into a deterministic "read in 30 seconds" summary.
- `features/portfolio/dailyBriefing/DailyBriefingCard.tsx` — the Daily Briefing UI card, first section on the Portfolio page's Positions tab.
- `docs/design/PI-0012-Portfolio-Review-Architecture.md` — the design doc PI-0012A implements Phase 1 of; PI-0012B/C/D (trailing performance, decision quality, polish) still to come.
- `lib/positionValuation/` — Marketable Pricing valuation layer (PI-0014): `computePositionValuation()` is the only exported function. Pure, no fetch/API/React, no knowledge of recommendations or promotion policy — `PositionValuation.liquidityTier` is `LiquidityTier | null` (null when `maxRisk` is missing/zero/negative, corrective closeout). `liquidityTrapTriggered` is NOT part of this module — it lives on `PositionObjectiveResult` in `lib/portfolio-intelligence/objectives/positionObjective.ts`, which is the Decision Engine and the sole owner of promotion/veto/trigger decisions. (An earlier pass had a separate `attachLiquidityTrapTrigger()` export here; it was removed once that field moved to the Decision Engine — there is no such export today.)
- `docs/design/PI-0014-Marketable-Pricing-Risk-Gating.md` — the condensed design doc for Phase 1 of the marketable-pricing fix; Phases 2-5 (full recommendation selector, priority ranking, Portfolio Review, health scoring) still to come.
- `docs/reviews/` — all implementation reports, `.md` (engineering) and `.docx` (product-owner) as needed.
- `lib/opportunity-engine/` — Opportunity Engine foundation (OE-0001): `evaluateOpportunityCandidate()`, `rankOpportunityCandidates()`, `adapters/decisionAnalysisAdapter.ts`. Pure comparison/ranking layer over already-computed `DecisionAnalysis` — never recomputes Opportunity Score, Decision Confidence, or Net Edge, never overrides a hard rejection.
- `components/opportunity-engine/BestOpportunitiesPanel.tsx` — read-only presentational surface for `OpportunityRecommendation[]`. Fully tested, intentionally unmounted (see loose end #22).
- `docs/design/OE-0001-Opportunity-Engine-Foundation.md` — the design doc for the Opportunity Engine foundation, including the corrected ranking policy (disposition-vs-disclosure separation, final display-order rules).
- `lib/paper-trading/` — PT-0001's Manual Paper Trading Sandbox domain: `validation.ts`, `pricing.ts`, `capital.ts`, `ledger.ts`, `idempotency.ts`, `audit.ts`, `service.ts` (the orchestration layer routes call), `adapters/portfolioIntelligenceAdapter.ts`, `persistence/{keys,locking,store}.ts`. Reads/writes only the new `paperTrading` field on the existing canonical `PaperAccount` record — never the Autopilot framework's own fields.
- `app/api/paper-trading/` — the paper-only API namespace (account, account/reset, positions, positions/[id]/close, positions/[id]/mark, intelligence). Thin routes; all domain logic is in `lib/paper-trading/service.ts`.
- `app/paper-trading/page.tsx` and `components/paper-trading/` — the Paper Portfolio UI: `PaperAccountSummary`, `PaperTicketForm`, `PaperCloseForm`, `PaperMarkForm`, `PaperPositionsList`, `PaperResetControl`, `PaperIntelligenceSummary`. The ticket/close/mark forms import only pure `lib/paper-trading` modules (never `service.ts` or the barrel `index.ts`), so `ioredis` is never pulled into the client bundle.
- `docs/design/PT-0001-Manual-Paper-Trading-Sandbox.md` — the design doc, including the accounting invariants, the reused-vs-new architecture decision, and the manual-mark-to-market limitation.
